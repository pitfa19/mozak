# ADR 0003: immutable snapshots and exact spans

Status: accepted for alpha planning.

Fetched or local content becomes a content-addressed immutable snapshot. Factual accepted claims require exact spans into snapshots. URLs and paths identify origins, not versions. Derived text and model summaries are not evidence by themselves.
